# 실제 공고 수집 가능성 메모

오퍼가디언은 실제 장학금/지원사업 데이터를 수집할 수 있다. 다만 안정성과 법적 리스크를 줄이기 위해 다음 순서로 접근한다.

## 1. 공식 API 우선

### 온통청년

- 청년정책/청년공간 정보를 Open API로 제공한다.
- 회원가입과 인증키 신청/승인이 필요하다.
- 결과 데이터는 XML 기반으로 제공된다.
- 활용: 청년지원금, 주거, 일자리, 교육/참여 정책 수집.

참고: https://www.youthcenter.go.kr/cmnFooter/openapiIntro/oaiGuide

### 정부24/보조금24 공공서비스 정보

- 국민에게 혜택을 제공하는 중앙부처, 지자체, 공공기관, 교육청 수혜서비스 정보를 Open API로 제공한다.
- 공공데이터포털 신청/API 키가 필요할 수 있다.
- 활용: 정부/지자체 지원금, 교육청/공공기관 혜택 수집.

참고: https://www.gov.kr/openapi

## 2. 공개 웹페이지/게시판 크롤링

API가 없는 대학 장학공지, 지자체 장학재단, 민간재단 공고는 공개 게시판 크롤링으로 수집한다.

원칙:

- 로그인/권한이 필요한 페이지는 수집하지 않는다.
- robots.txt, 이용약관, 저작권 정책을 확인한다.
- 과도한 요청을 피하고 rate limit을 둔다.
- 원문 URL, 수집 시각, 파일 해시를 저장한다.
- 공고 전문 복제보다 요약/메타데이터/원문 링크 중심으로 보여준다.

## 3. PDF/HWP/이미지 첨부 처리

장학금 공고는 PDF/HWP 첨부에 핵심 조건이 들어가는 경우가 많다.

처리 순서:

1. 첨부파일 다운로드
2. 해시로 중복 제거
3. PDF/HWP 텍스트 추출
4. OCR fallback
5. LLM 구조화 추출
6. 관리자 검수
7. 추천엔진 반영

## 4. MVP에 바로 붙일 수 있는 수집 커넥터

우선순위:

1. 온통청년 API 커넥터
2. 보조금24/대한민국 공공서비스 정보 API 커넥터
3. 대학 장학공지 HTML 게시판 커넥터
4. 지자체 장학재단 게시판 커넥터
5. 민간재단 PDF/HWP 첨부 커넥터

## 5. 필요한 환경변수 예시

```text
YOUTHCENTER_API_KEY=
PUBLIC_SERVICE_API_KEY=
GEMINI_API_KEY=
```

API 키가 없는 동안에는 현재 로컬 MVP처럼 시드 데이터와 수동 검수 큐를 사용하고, API 키가 발급되면 커넥터를 붙여 실제 공고로 교체한다.

## 6. 현재 MVP에 구현된 수집 흐름

현재 로컬 MVP에는 다음 동작이 구현되어 있다.

- `server/seed.mjs`에 수집 소스 등록
- 운영 검수 화면에서 전체/개별 소스 수집 실행
- API 소스는 키가 없으면 `NEEDS_KEY`로 표시
- HTML 소스는 Node 서버가 직접 공개 페이지를 fetch
- HTML anchor에서 장학/지원/청년/대학생 관련 링크 후보 추출
- 후보 공고를 `extractionQueue`에 등록
- 운영자가 승인하면 `opportunities`에 신규 공고 추가
- 추가된 공고는 프로필 기반 추천엔진에 즉시 반영

이 구조는 실제 서비스의 최소 운영 루프와 같다.

```text
source -> crawlRun -> candidate extraction -> admin review -> opportunity -> recommendation
```

## 7. 프로덕션 전환 시 보강해야 할 점

- 소스별 robots.txt/이용약관 점검 테이블
- 소스별 요청 주기와 동시성 제한
- 첨부파일 저장소(S3 등)
- PDF/HWP 텍스트 추출 워커
- Gemini 구조화 추출 프롬프트/스키마
- PostgreSQL 기반 중복 제거
- 관리자 검수 이력/필드별 변경 이력
- 원문 링크 기반 저작권/표시 정책
